@model IEnumerable<asociatie_proprietari.Models.ConsumApa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConsumApaRece)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsumApaCalda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apartament)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConsumApaRece)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsumApaCalda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apartament.ApartamentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
